# Comet-X Chrome Extension

البديل المفتوح المصدر لـ Perplexity Comet Browser - مدعوم بـ Azure OpenAI

## 🚀 التثبيت

### المتطلبات
- Node.js 18+
- npm أو yarn
- حساب Azure OpenAI مع نماذج مُنشرة

### خطوات التثبيت

```bash
# 1. انتقل إلى مجلد الإضافة
cd comet-x-extension

# 2. ثبت الاعتماديات
npm install

# 3. ابني الإضافة
npm run build

# 4. حمّل الإضافة في Chrome
#    - افتح chrome://extensions
#    - فعّل "وضع المطور"
#    - انقر "تحميل إضافة غير مُعبأة"
#    - اختر مجلد 'dist'
```

## ⚙️ الإعداد

1. افتح إعدادات الإضافة
2. أدخل:
   - **نقطة النهاية**: `https://uaenorth.api.cognitive.microsoft.com/`
   - **مفتاح API**: مفتاح Azure OpenAI الخاص بك
   - **النموذج**: اختر من `gpt-4o`, `gpt-4.1`, أو `o3-mini`
3. انقر "اختبار الاتصال" للتأكد
4. احفظ الإعدادات

## 🎯 الميزات

- ✅ **Side Panel**: لوحة جانبية للمحادثة
- ✅ **قائمة السياق**: انقر بالزر الأيمن لـ:
  - اسأل Comet-X
  - اشرح هذا
  - ترجم إلى العربية
  - لخص الصفحة
- ✅ **اختصارات لوحة المفاتيح**:
  - `Ctrl+Shift+X`: فتح Comet-X
  - `Ctrl+Shift+A`: سؤال AI عن النص المحدد
- ✅ **سياق الصفحة**: يفهم محتوى الصفحة الحالية
- ✅ **دعم RTL**: واجهة عربية كاملة

## 📁 هيكل المشروع

```
comet-x-extension/
├── manifest.json           # Manifest V3
├── package.json
├── vite.config.ts
├── tsconfig.json
├── tailwind.config.js
├── _locales/               # الترجمات
│   ├── ar/messages.json
│   └── en/messages.json
├── icons/                  # الأيقونات
└── src/
    ├── types/              # TypeScript types
    │   └── index.ts
    ├── lib/                # مكتبات
    │   └── azure-client.ts # Azure OpenAI client
    ├── styles/
    │   └── global.css      # Tailwind styles
    ├── background/         # Service Worker
    │   └── index.ts
    ├── content/            # Content Script
    │   └── index.ts
    ├── sidepanel/          # Side Panel UI
    │   ├── index.html
    │   ├── index.tsx
    │   └── SidePanel.tsx
    └── options/            # Options Page
        ├── index.html
        ├── index.tsx
        └── OptionsPage.tsx
```

## 🔧 التطوير

```bash
# وضع التطوير مع المراقبة
npm run dev

# بناء للإنتاج
npm run build:prod

# فحص الأنواع
npm run typecheck
```

## 🔑 النماذج المدعومة

| النموذج | الوصف | الاستخدام |
|---------|-------|----------|
| `o3-mini` | نموذج التفكير المتقدم | المهام المعقدة |
| `gpt-4.1` | أحدث GPT-4 | متوازن |
| `gpt-4o` | GPT-4 Omni | سريع |

## 📝 الرخصة

MIT License - مفتوح المصدر بالكامل

---

**صُنع بـ 💙 كبديل مجاني ومفتوح لـ Perplexity Comet**
